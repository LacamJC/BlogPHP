CREATE TABLE posts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    titulo TEXT NOT NULL UNIQUE,
    autor TEXT NOT NULL,
    conteudo TEXT NOT NULL,
    data_publicacao TEXT NOT NULL
);

ALTER TABLE nome_tabela ADD COLUMN nome_nova_coluna tipo_dados [opções_de_coluna];
ALTER TABLE posts ADD COLUMN hyperlink TEXT;
